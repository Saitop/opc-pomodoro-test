# Pomodoro Studio

A small Vite React single-page app for planning focused work sessions.

## Scripts

```bash
pnpm install
pnpm build
pnpm dev
```

The production build outputs static assets to `dist`.
